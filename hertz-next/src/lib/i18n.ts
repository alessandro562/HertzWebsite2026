/**
 * Lingua per le sole superfici bilingui (bookings + registrazione evento).
 * Default EN in tutto il sito; l'IT si attiva solo dove c'è un LangToggle.
 */
export type Lang = 'en' | 'it'

export const DEFAULT_LANG: Lang = 'en'
